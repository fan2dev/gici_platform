import 'package:flutter/material.dart';

import 'app_config.dart';

/// GICI white-label generic configuration.
const giciConfig = AppConfig(
  appName: 'GICI',
  bundleId: 'com.gici.app',
  apiUrl: 'http://localhost:8080/',
  seedColor: Color(0xFF0A6E8A),
  logoAsset: null,
);
