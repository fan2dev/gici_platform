# Auth session DTO (transfer object, no database table)

class: AuthSession

fields:
  appUserId: UuidValue
  organizationId: UuidValue?
  email: String
  role: String
  firstName: String
  lastName: String
