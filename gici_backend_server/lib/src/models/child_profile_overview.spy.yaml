# Child profile overview DTO (transfer object, no database table)

class: ChildProfileOverview

fields:
  child: Child
  activeClassroomId: UuidValue?
  activeClassroomName: String?
  guardianUserIds: List<UuidValue>
  guardianDisplayNames: List<String>
  documentsCount: int
  reportsCount: int
  lastHabitAt: DateTime?
