# Child timeline item DTO (transfer object, no database table)

class: ChildTimelineItem

fields:
  childId: UuidValue
  eventAt: DateTime
  eventType: String
  title: String
  description: String?
  referenceType: String?
  referenceId: String?
