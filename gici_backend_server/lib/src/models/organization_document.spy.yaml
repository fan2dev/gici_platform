class: OrganizationDocument

table: organization_document

fields:
  organizationId: int
  fileAssetId: int
  title: String
  description: String?
  visibility: String
  createdByUserId: int
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime?

indexes:
  orgdoc_org_idx:
    fields: organizationId
  orgdoc_file_idx:
    fields: fileAssetId
  orgdoc_visibility_idx:
    fields: visibility
